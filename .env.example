APP_NAME=ai-upgrade-recommendation-engine
ENVIRONMENT=local
DATABASE_URL=sqlite:///./recommendations.db

# Optional — engine works without these (deterministic explanations)
LLM_ENABLED=true
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini

HUMAN_REVIEW_RISK_THRESHOLD=0.6
HIGH_CRITICALITY_REQUIRES_REVIEW=true
